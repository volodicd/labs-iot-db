spring:
  datasource:
    url: jdbc:mysql:/database-server-01.database.windows.net:3306/lab5_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: volodymyr
    password: Cthdth1r.23
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    # Show or not log for each sql query
    show-sql: true
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mvc:  # the fix for swagger
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: 8080
